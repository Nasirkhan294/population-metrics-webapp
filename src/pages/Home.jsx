import styles from '../styles/Home.module.css';

const Home = () => (
  <div>
    <div className={styles.banner}>
      <input type="text" placeholder="search by coutry name..." />
    </div>
  </div>
);

export default Home;
